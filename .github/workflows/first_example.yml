name: My First workflow
on: [push]
jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Welcome message
        run: echo "Hello, welcome to my first GitHub Actions workflow!"

      - name: List files
        run: ls

      - name: Read file
        run: cat README.md